# Crack — 历史目录 / Legacy folder

本目录曾存放 OEM 安装器解出的文件与 INF 转换工具（Transform-INFs.ps1），
因版权剥离现已清空，仅保留占位结构。
This folder used to hold files extracted from the OEM installer plus an INF transformation
tool (Transform-INFs.ps1). It is now emptied for copyright reasons (structure kept as a placeholder).

**当前为空是正常的，不影响任何功能。**
**Being empty is normal; it does not affect any feature.**

如需自备内容放这里（可选）/ If you want to place your own files here (optional):
- `Transform-INFs.ps1`   INF 转换工具（把 OEM INF 去 SUBSYS 转 generic）
- 从 OEM 安装器解出的文件（本工具不依赖它们）
